{
  "schema": "load-scenario/v1",
  "name": "uncached",
  "description": "Steady load with the retrieval cache disabled (no REDIS_URL), so every question pays full retrieval cost. Baseline for the cached comparison; runs on the offline fallback stack or Compose.",
  "warmup": "15s",
  "stages": [
    {"duration": "120s", "users": 16, "spawn_rate": 4}
  ],
  "env": {},
  "pass_fail": {
    "max_error_rate": 0.01,
    "max_p95_ms": 2500
  }
}
